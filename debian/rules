#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
# export DH_VERBOSE=1

export PYBUILD_NAME=wazo-service
export PYBUILD_DISABLE=test

%:
	dh $@ --with python3 --buildsystem=pybuild
